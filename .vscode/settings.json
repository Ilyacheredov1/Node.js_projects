{
    "files.exclude": {
       "**/*.js": {"when": "$(basename).tsx"},
       "**/**.js": { "when": "$(basename).ts" },
       "**/*.map": {"when": "$(basename).map"}
   },
   "typescript.tsdk": "node_modules/typescript/lib",
 }